{
  "name": "@xodarap/prisma",
  "private": true,
  "sideEffects": false,
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.7.0",
    "prisma": "^6.7.0",
    "zod": "^3.24.3",
    "zod-prisma-types": "^3.2.4"
  },
  "scripts": {
    "generate": "prisma generate --schema=./schema/schema.prisma",
    "reset": "prisma migrate reset --schema=./schema/schema.prisma",
    "dev": "prisma migrate dev --create-only --schema=./schema/schema.prisma"
  }
}
